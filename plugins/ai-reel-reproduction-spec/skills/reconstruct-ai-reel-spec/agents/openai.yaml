interface:
  display_name: "AIリール再現仕様書"
  short_description: "公開URLから伸びる機構と制作仕様を一括作成"
  default_prompt: "Use $reconstruct-ai-reel-spec with this public account URL to produce an evidence-backed, original short-video reproduction specification."
policy:
  allow_implicit_invocation: true

